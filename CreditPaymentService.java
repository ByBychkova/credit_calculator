public class CreditPaymentService {

    public int calculate(int sc, int np, double st) {

        int result;

        Math.pow(a1, b2);
        int chek = sc * (st / 1 - Math.pow(a1, b2)) = (sc * st) / 1 - (1 / Math.pow(a2, b3)) = sc(st + (st / (Math.pow(a1, b2) - 1);
        result = (int) chek;
        return result;

    }
}